# Calino — example environment file
#
# Copy this file to `.env.local` and adjust the values for your deployment.
# `.env.local` is git-ignored and is the standard Vite location for secrets
# and per-build overrides.

# ── SEO / Metadata ──────────────────────────────────────────────

# Public URL where Calino is served. Used for SEO meta tags, Open Graph
# cards, canonical link, and the structured-data JSON-LD block in
# index.html. Must NOT include a trailing slash.
#
# Example: VITE_SITE_URL=https://calendar.example.com
VITE_SITE_URL=https://calino.io

# Optional: automatically connect a same-origin DAV route authenticated by the
# browser session at the reverse proxy. Calino sends no Authorization header.
# VITE_CALINO_BROWSER_SESSION_DAV_URL=/dav/
# VITE_CALINO_BROWSER_SESSION_ACCOUNT_NAME=Calendar
# VITE_CALINO_BROWSER_SESSION_DEFAULT_CALENDAR_NAME=Personal

# Optional: prefill the proxy used by browser-managed WebCal subscriptions.
# The proxy must implement Calino's encoded-origin GET contract.
# VITE_CALINO_WEBCAL_PROXY_URL=/subscriptions/proxy

# Optional: expose server-managed, one-way subscriptions in Sync settings.
# The endpoint must implement Calino's managed-subscription JSON contract.
# VITE_CALINO_MANAGED_SUBSCRIPTIONS_URL=/subscriptions/api

# ── Deployment customization ────────────────────────────────────

# GitHub repo slug (shown in app footer / about links)
# Default: ivan-malinovski/Calino
# CALINO_GITHUB_REPO=your-user/your-repo

# Contact email (shown in app footer)
# Default: calendar@malinov.ski
# CALINO_CONTACT_EMAIL=you@example.com

# Enable service worker for offline support (requires self-hosting
# with Service-Worker-Allowed header). Default: false
# CALINO_ENABLE_SW=true
